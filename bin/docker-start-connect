#!/bin/bash
# docker-start-connect --- Start docker image and connect to ssh server on it.

if [[ "$1" != "" ]]; then
    IMAGE="$1"; shift
else
    IMAGE="mckoss/sshd"
fi

docker run -p 22 -d $IMAGE

sleep 1
docker-connect
