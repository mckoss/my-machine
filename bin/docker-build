#!/bin/bash
# docker-build --- Build a docker image from a Dockerfile
BIN_DIR="$(cd $(dirname ${BASH_SOURCE[0]}) && pwd)"
source $BIN_DIR/.envrc
source $BIN_DIR/setup-funcs.sh

docker build -t ${DOCKER_USER}/$1 $PROJ_DIR/dockerfiles/$1
