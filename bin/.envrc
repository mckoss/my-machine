PROJECT=my-machine

PROJ_DIR="`dirname $BIN_DIR`"
DOWN_DIR="$HOME/Downloads"

if [ `uname` == "Darwin" ]; then
    PLATFORM="Mac"
elif [[ `uname` == *W32* ]]; then
    PLATFORM="Windows"
    SUDO=""
else
    PLATFORM="Linux"
fi

echo "I think you are running $PLATFORM."
